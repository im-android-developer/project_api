services:
  - type: web
    name: system-project-api
    runtime: python
    pythonVersion: "3.12.3"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --threads 100 -w 1 app:app
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
